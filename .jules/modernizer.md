## 2025-02-14 - Type hints modernization
**Learning:** Python 3.10+ projects can use PEP 585/604 native equivalents instead of verbose typing classes (e.g. `Union`, `Optional`, `Tuple`, `Dict`).
**Action:** Used `str | None` instead of `Optional[str]` and `tuple` instead of `Tuple`.
