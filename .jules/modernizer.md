## 2024-05-23 - Modernize type hints
**Learning:** Python 3.10+ supports modern native type hints without importing `Union`, `Optional`, `Tuple`, `Dict` from `typing`.
**Action:** Use native type hints `|` for `Union`/`Optional` and built-in generics like `tuple`, `dict`.
