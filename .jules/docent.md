## 2024-05-30 - Add docstrings to public API and fix doc formatting
**Learning:** Scikit-learn estimators should have fully documented standard public APIs (fit, predict, score, etc.), and default values in docstrings should be formatted consistently without spaces around the equals sign.
**Action:** Always ensure standard methods have numpydoc-style docstrings and adhere to project-specific parameter formatting.
