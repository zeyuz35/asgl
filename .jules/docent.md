## 2026-05-17 - Fixed Typo in documentation of lambda1 parameter\n**Learning:** Simple typos like 'defaul' instead of 'default' easily bypass programmatic checks if not explicitly checked by a documentation spell checker.\n**Action:** Use ripgrep with regular expressions to search for common misspelling patterns of known keywords within asgl and tests directories when performing general typo checks.
